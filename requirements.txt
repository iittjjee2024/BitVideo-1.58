# ---------------------------------------------------------------------------
# BitVideo-1.58 runtime requirements
#
# Hard requirements (the package will not import without these)
# ---------------------------------------------------------------------------
torch>=2.4
numpy>=1.24
safetensors>=0.4
pyyaml>=6.0

# ---------------------------------------------------------------------------
# Soft requirements.  Every one of these is imported lazily behind a
# try/except in bitvideo, and a pure-PyTorch fallback exists.  Install the
# ones you need.
# ---------------------------------------------------------------------------
# Video I/O (mp4 / gif writing, dataset decoding)
imageio>=2.31
imageio-ffmpeg>=0.4.9
av>=11.0

# Kernel back-ends
# triton>=3.0            ; Linux only -- optional Triton BitLinear kernels
# flash-attn>=2.5        ; Linux only -- optional FlashAttention-2/3 backend

# Training / logging
# tensorboard>=2.15
# wandb>=0.16
# omegaconf>=2.3
# hydra-core>=1.3
# lpips>=0.1.4

# HuggingFace interop (text encoders, diffusers compatibility)
# diffusers>=0.30
# transformers>=4.44
# accelerate>=0.33

# Dev / test
# pytest>=8.0
# ruff>=0.6
# einops>=0.8
